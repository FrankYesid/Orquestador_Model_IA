# Variables de entorno para Docker Compose
# Copia este archivo como .env en el directorio docker/

# Usuario y grupo de Airflow (usa tu UID/GID en Linux/Mac)
# En Windows, puedes usar 50000
AIRFLOW_UID=50000
AIRFLOW_GID=0

# Credenciales de la interfaz web de Airflow
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow

# Directorio del proyecto (ruta absoluta)
AIRFLOW_PROJ_DIR=.

# Configuración de PostgreSQL
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow
POSTGRES_DB=airflow
